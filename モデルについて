＊モデルとは？
目的に合わせて必要なふるまい、性質を表現したもの。
複雑な機能があっても、重量しか必要でなければ、20(kg)という情報(数値)だけのモデルになる。


＊MBSEのモデルを以下のように分類する。
1　スタンドアローン(stand alone)
MBSEにのみ用いるもの。サブシステムの設計とは独立している。

2　コンペア(compare)
サブシステムの設計データと比較して一致することは確認するが、一方からもう一方を生成することはしないもの。

3　マザー(mother)
MBSEのモデルからサブシステムの設計データを生成するもの。(足りない情報はコメントなどに埋め込む)

4　チャイルド(child)
サブシステムの設計データからMBSEのモデルを生成するもの。

＊DSL
様々なシステム(機械要素、電子装置、物理モデルなど)で汎用的、効率的にモデルを表現することは困難なため、独自にモデルの表現方法を定義して、
それを変換またはそれに変換する。
独自の表現方法のためにDSLを作る。



