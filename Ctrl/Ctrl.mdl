# property
(setq author "田中一郎")
(setq date  "2023/05/06")
(setq name "温度制御")
(setq ID "0001")
(setq VER "A")

###########################
(setq staus OFF) (name staus "制御状態")

###########################
(defun turn_on()
  (setq Objects.heater ON)
  (setq status ON)
)

(defun turn_off()
  (setq Objects.heater  OFF)
  (setq status OFF)
)
